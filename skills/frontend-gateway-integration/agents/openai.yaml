display_name: Frontend Gateway Integration
short_description: Implement project-prefixed gateway integration for auth, SQL, platform admin, and encrypted payload requests.
default_prompt: Integrate frontend features with this repo's multi-project gateway, including global auth, /api/gw project routes, /api/platform admin flows, encrypted payloads, and role-based UI controls.
