display_name: Frontend Gateway Integration
short_description: Implement frontend integration for auth, SQL, audit logs, admin user APIs, and encrypted payload requests.
default_prompt: Integrate a frontend feature with this repo's SQL Gateway backend, including token auth, optional encrypted payloads, and role-based admin UI flows.
